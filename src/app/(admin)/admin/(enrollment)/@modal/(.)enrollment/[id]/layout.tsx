export const metadata = {
    title: "Enrollment Details | Admin Portal",
    description: "Admin Portal Enrollment",
  };
  
  export default function EnrollmentLayoutId({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <section>{children}</section>;
  }
  