export const metadata = {
    title: "Manage Enrollment | Admin Portal",
    description: "Admin Portal Enrollment Management",
  };
  
  export default function ManageEnrollmentLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <section>{children}</section>;
  }
  