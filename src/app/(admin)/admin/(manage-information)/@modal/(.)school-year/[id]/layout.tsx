export const metadata = {
  title: "School Year Details | Admin Portal",
  description: "Admin Portal School Year",
};

export default function SchoolYearLayoutId({
  children,
}: {
  children: React.ReactNode;
}) {
  return <section>{children}</section>;
}
