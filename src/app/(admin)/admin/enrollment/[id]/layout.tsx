export const metadata = {
    title: "Manage Enrollment Details| Admin Portal",
    description: "Admin Portal Enrollment Management Details",
  };
  
  export default function ManageEnrollmentLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <section>{children}</section>;
  }
  