import { FC } from "react";

export const metadata = {
  title: "Enrollment | Admin Portal",
  description: "Admin Portal Enrollment",
};

interface adminEnrollProps {}

const AdminEnrollment: FC<adminEnrollProps> = ({}) => {
  return <div>Manage Student Enrollment</div>;
};

export default AdminEnrollment;
