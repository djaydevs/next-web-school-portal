import { FC } from "react";

export const metadata = {
  title: "Document Request | Admin Portal",
  description: "Admin Portal Document Request",
};

interface adminDRProps {}

const AdminDocumentRequest: FC<adminDRProps> = ({}) => {
  return <div>Admin Document Request</div>;
};

export default AdminDocumentRequest;
