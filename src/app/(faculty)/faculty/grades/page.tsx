import { FC } from "react";

export const metadata = {
  title: "Report Card | Faculty Portal",
  description: "Faculty Portal Report Card",
};

interface facultyRCProps {}

const FacultyReportCard: FC<facultyRCProps> = ({}) => {
  return <div>Add Student Grades</div>;
};

export default FacultyReportCard;
