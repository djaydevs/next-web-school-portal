export const metadata = {
    title: "Report Card Details | Faculty Portal",
    description: "Faculty Portal Report Card Details",
  };
  
  export default function ManageGradeIdLayout({
    children,
  }: {
    children: React.ReactNode;
  }) {
    return <section>{children}</section>;
  }
  