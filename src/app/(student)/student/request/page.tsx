import { FC } from "react";

export const metadata = {
  title: "Document Request | Student Portal",
  description: "Student Portal Document Request",
};

interface studDRProps {}

const StudentDocumentRequest: FC<studDRProps> = ({}) => {
  return <div>Document Request</div>;
};

export default StudentDocumentRequest;
